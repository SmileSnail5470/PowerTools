# ONNX Runtime inference dependencies (torch-free)
onnxruntime>=1.15.0
numpy>=1.22.0
Pillow>=9.0.0
scipy>=1.7.0

# Conversion dependencies (requires PyTorch, run separately on a machine with torch)
# torch>=2.0.0
# torchvision>=0.15.0
# diffusers>=0.21.0  # Only needed for SD export
# transformers>=4.30.0
# mmcv>=2.0.0  # Only needed for original FCNet model definition
